# PageEmber · 设计备忘录
## "在读上下文" + "想读书单" + "讲故事的月统计"

> 这份文档是 2026 年 4 月某次 brainstorm 的成果。
> 不是 spec，是给未来自己的种子。
> 真要做的时候，先重读、再质疑、再动手。

---

## 一、核心洞察（先于任何功能）

最初的问题是"要不要和豆瓣联系"。最终发现：

**想要的不是豆瓣，是豆瓣那种"把书放进上下文里"的感觉。**

PageEmber 现在的产品支柱：
1. 过程激励（沙漏、计时、streak）
2. 过程结算（session 结束、月统计）
3. 回顾反思（弃读分析）

要加的第 4 根支柱：
4. **在读上下文** —— 在你正读这本书的当下，给你"周边"

豆瓣的相关推荐对所有人都一样；
PageEmber 的相关上下文**只关于你自己**——这是豆瓣给不了的。

---

## 二、产品语气（最重要的资产）

PageEmber 已经有自己的语气了，要保护好：

- **温和的、第二人称疑问式的**
- **把书当成"伙伴"而不是"任务"**
- 不用 XP、不用奖章、不用"加油打卡"
- 已有例子："读哪本？" "为什么放下？" "看看它们之间有什么共同点" "读完了就是读完了"

将来 marketing 的方向（不是 spec，是定调）：
> 一个会和你聊你正在读的书的小伙伴
> 它问你的问题，豆瓣不会问

---

## 三、想读书单（Wishlist）—— 第四种状态

### 设计原则

新增 status: `wishlist`（想读）

**手动添加，故意有摩擦。** 理由：
- 防止"看到啥都加进来"导致书单膨胀，反而压力大不读了
- 添加过程本身是个"我真的想读吗"的思考时刻
- 不做豆瓣导入的一键功能（即使 18 年想读列表很可观也不导）—— 18 年累积的想读多半已经过期了

### 数据字段

book 增加：
- `status: 'wishlist'`
- `addedToWishlistAt: timestamp`（用于"等了 X 个月"的文案）

### 添加流程

添加书的 modal 增加一个选择：
- "📖 直接开始读"（默认，进 reading）
- "📚 先放想读"（进 wishlist）

进度字段在选"想读"时隐藏。

### 想读列表展示

在管理页加一个 tab/section 展示。每本书显示：
- 书名、作者
- "在想读里 X 天/月"
- 一键"开读"按钮（status: wishlist → reading）

---

## 四、在读上下文 —— 三个时机

### 时机 B（最先做）：添加书时的预警/呼应

输入作者后，下方浮现小字：

```
作者：马伯庸
        ↳ 你读过他的 2 本，1 本读完，1 本放下
```

如果是译者：

```
译者：陶立夏
        ↳ ⚠️ 你之前放下过她翻译的 2 本书
```

把现有的"放下的书"分析从**事后诸葛亮**变成**事前预警**。

### 时机 C（次优先）：完读后的"下一本"

读完一本书后，在结算页最后一层显示"下一本"提示。

**优先级层级（按顺序匹配，命中即停）：**

1. **在读 + 进度高的书**
   - percent ≥ 75%："《XXX》已经读了 75% 了，要不要一鼓作气把它收尾"
   - percent 60-74%："《XXX》已经读到一半多了，要不要接着它"

2. **想读书单里等了很久的**
   - < 1 个月：不提（太新）
   - 1-3 个月："《XXX》在你的想读里有一阵了"
   - 3-12 个月："《XXX》在你的想读里等了 X 个月了呢"
   - ≥ 12 个月："《XXX》已经在你想读里待了一整年"

3. **同作者关联**
   - "你刚读完了马伯庸，他的《两京十五日》还在你的想读里"
   - 仅当 wishlist 里有同作者的书时显示

4. **兜底：什么都没匹配上 → 不显示"下一本"层**
   - 不要硬凑文案。"接下来呢？" 这种空话不如不说。

**交互**：每个推荐卡片带一个"一键开读"按钮，点了直接：
- 把那本书 status 设为 reading（如果是 wishlist）
- 进入选书 → 计时流（不需要再选一次）

### 时机 A（最后做）：计时开始前的"接续卡片"

选完书、计时开始前停留 2-3 秒：

```
《长安的荔枝》

你和这本书的第 5 次见面
上次：3 天前，读了 28 分钟
进度：62%

——————

你读过马伯庸的另外 2 本：
《显微镜下的大明》（4小时12分读完）
《两京十五日》（读到 45% 放下了）
```

可点击跳过。

---

## 五、月统计的"读信感"重做

### 设计原则

**不是仪表盘，是日记/书信。**

- 卡片式
- 像"远方笔友"在和你说话
- 一屏一张卡，向下滚动展开
- 没匹配上的卡片直接不显示，**绝不硬凑**
- 标题不是 "2026 年 4 月"，而是根据数据生成的一句话标题
  - "和马伯庸的月份"
  - "回到《XXX》的月份"
  - "完读 3 本的月份"
  - "短暂尝试的月份"
  - 没有强特征时退回 "2026 年 4 月"

### 卡片模板

**卡片 1：连续性**
- 触发：当月有 ≥ 5 天连续阅读
- 文案："X 月 X 日到 X 月 X 日，你 X 天都读了书 / 养成了不错的习惯"

**卡片 2：高光时刻**
- 触发：当月最长单日 ≥ 60 分钟
- 文案："X 月 X 日你读了 X 小时 X 分 / 是这个月读得最久的一天 / 读书是不是让你觉得平静"
- 第二个月开始最后一句变成"还是觉得平静吗"，产生连续性

**卡片 3：投入瞬间**
- 触发：当月有单次 session ≥ 60 分钟 + 进度推进 ≥ 20%
- 文案："《XXX》你 X 月 X 日一口气读了 X 小时 X 分 / 从 X% 读到了 Y% / 这本书很吸引你吧"

**卡片 4：接力建议**
- 触发：当月完读书的作者在 wishlist 里有同作者的另一本
- 文案："你的想读书单里有 X 本 [作者] 的书 / 读完《XXX》后，要不要试试《YYY》"
- 带"一键开读"按钮

**卡片 5：被冷落的伙伴**
- 触发：当月没读、status='reading'、lastReadAt 在 30-60 天前
- 文案："《XXX》上次见面是 X 月 X 日 / 它还在等你"
- > 60 天就转成"要不要放下"的弃读引导，不用"等你"这种期待感的话

### 视觉方向（待具体设计时再展开）

- 像翻信笺
- 卡片之间有节奏，不是平铺
- 配色保持现有体系（不要为了"信件感"换风格）

---

## 六、明确不做的（防止设计文档惯性）

- ❌ 豆瓣 API 集成（豆瓣没开放 API，不做爬虫）
- ❌ 豆瓣历史数据导入（读过的几百本会污染当下的书架；想读列表 18 年累积也已过期）
- ❌ 输入书名自动提取作者（中文书覆盖差、多一层失败可能、还是要手改）
- ❌ 作者画像 / "你读得最多的作者"（启动条件未到：要至少 5 个作者各 ≥ 2 本书才有意义。先记着，到了再做）
- ❌ XP 系统（已经拔掉了，别又长回来）
- ❌ 强激励填作者/译者（让"缺失看起来亏了"，不要"填了奖励"）

---

## 七、做的顺序建议

按"价值/工程量"排：

**Stage 7（建议）：想读书单 + 完读后下一本提示**
- wishlist 状态、addedToWishlistAt 字段、迁移
- 添加书 modal 增加"先放想读"选项
- 管理页增加想读 section
- 完读结算最后一层（按四级优先级）+ 一键开读

理由：这一组功能是互相依赖的——没有 wishlist 就没有"等了 X 个月"，没有"等了 X 个月"就没有完读后的引导。要做就一起做。

**Stage 8：添加书时的同作者/译者提示**
- 时机 B 的预警/呼应

**Stage 9：月统计重做成卡片式叙事**
- 月份标题生成
- 卡片模板（5 张起步，按数据匹配）
- 视觉：信件感

**Stage 10（可选）：计时前的接续卡片**
- 时机 A
- 用过 7-9 之后再判断要不要做

---

## 八、做之前要重新问的问题

每次新 stage 开工前，先回答：

1. 这个功能是不是又一个"豆瓣集成"式的设计文档惯性？
2. 它是单纯关于"我自己的数据"，还是依赖外部？依赖外部的先放放。
3. 它的语气是不是和 PageEmber 现有的疑问式、伙伴式一致？
4. 它有没有"硬凑"的部分？没匹配上的内容能不能干脆不显示？
