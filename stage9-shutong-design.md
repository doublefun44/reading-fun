# Stage 9:书童(守火人)· 设计决策与实现计划

> 本文档是本 Stage 的真相源。所有设计决策已确认,不要重新发起设计讨论,不要扩展范围。
> 按步骤实现,每步完成后停下,等我在本地验证通过后再进行下一步。

## 项目背景(给 Claude Code 的最小上下文)

PageEmber(页焰)是一个纯 vanilla HTML/CSS/JS + localStorage 的 iPad 读书追踪 PWA。
文件:`index.html` / `app.js` / `storage.js` / `style.css`。localStorage 主 key 为 `rpg.books`(历史名,保留)。

产品哲学:书是陪伴,不是任务。禁止引入游戏化语言、成就框架、责怪性文案。

## 工作规则(必须遵守)

1. **不用行号定位代码**。用函数名和可搜索的字符串锚点(如 `renderStreakRing`、`#hourglass`)。
2. **一步一停**。每个 Step 做完就停,给出验证方法,等确认后再继续。
3. **不扩展范围**。只实现本文档写明的内容,不加额外功能、提示、测试。
4. **不改动 streak 环现有逻辑**。`renderStreakRing`、熄灭动画、emoji 切换全部保持原样,书童是环外新增元素。
5. **不提交代码**。我会在 VS Code + Live Server 本地测试后自己 commit。
6. 深色背景为 `#1a1a2e`,正文字色 `#eaeaea`,现有代码里的日期字符串用 `toLocaleDateString('sv')`,保持一致。

## 设计决策(已全部确认)

### 概念

书童是"守火人":首页 streak 环里的那团火是他守的火。他不奖励也不惩罚,只如实反映陪伴状态。

### 状态机

状态由**距最近一次阅读 session 的天数**驱动(按本地日历日计算,用 `toLocaleDateString('sv')` 对齐现有代码):

| 状态 | 条件 | 视觉 |
|------|------|------|
| 精神 | 今天有阅读记录 | 坐直,眼睛睁开(两个圆点),轻微呼吸起伏 |
| 平静 | 1 天没读 | 正常坐姿,眼睛略小 |
| 犯困 | 2 天没读 | 头前倾,眼睛为半闭横线,1 个 z 缓缓飘起 |
| 睡着 | 3 天及以上没读 / 从无任何 session | 趴伏,眼睛闭合,3 个 z 缓缓飘起 |

特殊状态:**陪读**——计时进行中,低头读书姿态,覆盖以上四态。

这套状态与火焰状态天然对齐(今天读过=火亮/1 天=暗火/2 天=刚熄/3 天+=冷木桩),不需要额外联动逻辑,各算各的即可。

### 首页

- 书童坐在 streak 环**右下角外侧**,面朝火(朝左上),高约 70–90px。
- 不遮挡环、hint 文案和按钮;iPad 竖屏下布局不跳动。

### 计时页

- 现有沙漏缩至 **2/3 大小,移到左上**。
- 书童为**原沙漏尺寸的 1/3,放右下**,低头陪读姿态。两者对角布局。

### 形象风格

- 简笔线条 SVG:细线勾轮廓,不填色(或透明/背景色填充),线色用 `#eaeaea` 一系,配深色背景。
- 不用 emoji,不用图片资源,内嵌 SVG。

### 动效

1. **微动效**(CSS 动画):
   - 精神:轻微呼吸起伏(缓慢 scale/translateY 循环)。
   - 犯困/睡着:z 字缓缓上飘、淡出、循环。
2. **醒来过渡**:
   - 触发条件:开始计时时,书童处于犯困或睡着状态。
   - 播放位置:进入计时页后,在右下角陪读位上播——睡眼惺忪出场 → 揉眼睛 → 进入低头陪读姿态,约 1 秒。
   - 不需要一次性标记:计时结束后今天已有阅读记录,状态自然变为精神,下次不会重播。
   - 首页不播醒来动画,点"开始阅读"不加任何延迟。

### 语气

- 醒来不配任何文案。不出现"你终于来了"这类责怪性表达。醒来本身就是全部反馈。
- 本 Stage 不新增任何文案字符串。

## 实现步骤

### Step 1:状态计算(纯数据层)

在 `app.js` 增加 `getAttendantState()`:
- 读 `storage.getSessions()`,取最近一次 session 的本地日期,算与今天相差的天数。
- 返回 `'awake' | 'calm' | 'drowsy' | 'asleep'`(0 / 1 / 2 / ≥3 天,无 session 记为 asleep)。
- 不做任何 DOM 操作。

**验证**:控制台调用 `getAttendantState()`;临时改写 localStorage 里 session 的 startTime 模拟 0/1/2/5 天前,确认四态返回正确,测完还原数据。

### Step 2:首页静态书童(四姿态)

- `index.html` 首页 streak 环附近新增内嵌 SVG,包含四个姿态(可用四组 `<g>` 切换显隐,或一组元素改属性,实现方自选,以简单为准)。
- `app.js` 在首页渲染时根据 `getAttendantState()` 切换姿态。
- `style.css` 定位到环右下角外侧。

**验证**:同 Step 1 的数据模拟法,刷新首页看四个姿态是否正确显示、位置是否不遮挡现有元素。

### Step 3:计时页布局 + 陪读姿态

- 沙漏缩至 2/3、移左上(优先用 CSS transform/布局调整,不动 `#hourglass` 内部 SVG 结构)。
- 右下新增陪读姿态书童(约原沙漏 1/3 大小)。

**验证**:开始一次计时,看布局;结束计时回首页,书童应为精神状态。

### Step 4:微动效

- 精神呼吸起伏;犯困 1 个 z、睡着 3 个 z 上飘循环。CSS 动画实现。

**验证**:数据模拟法逐态检查动效。

### Step 5:醒来过渡

- 开始计时时若状态为 drowsy/asleep,进计时页后播约 1 秒醒来过渡,结束后定格陪读姿态。

**验证**:模拟 2 天/5 天没读 → 开始计时,看过渡;紧接着再开一次计时,不应重播。

### Step 6:回归

- 首页 streak 环三态(empty/short/done)+ 熄灭动画均不受影响。
- 结算页、书籍管理、详情页无布局异常。
- 提醒:部署后 iPad PWA 需移除再重新添加主屏图标;`sw.js` 缓存版本号要更新。
