# Stage 10:告别信 · 设计决策与实现计划

> 本文档是本 Stage 的真相源。所有设计决策已确认,不要重新发起设计讨论,不要扩展范围。
> 按步骤实现,每步完成后停下,给出验证方法,等我在本地验证通过后再进行下一步。

## 项目背景(给 Claude Code 的最小上下文)

PageEmber(页焰)是一个纯 vanilla HTML/CSS/JS + localStorage 的 iPad 读书追踪 PWA。
文件:`index.html` / `app.js` / `storage.js` / `style.css`。localStorage 主 key 为 `rpg.books`(历史名,保留)。

产品哲学:书是陪伴,不是任务。读完的书不是战利品,是送走的朋友。禁止游戏化语言、责怪性文案、强行煽情。

## 工作规则(必须遵守)

1. **不用行号定位代码**。用函数名和可搜索的字符串锚点(如 `showFinishLayer`、`renderBookDetail`、`recapFinishLayer`)。
2. **一步一停**。每个 Step 做完就停,等确认后再继续。
3. **不扩展范围**。只实现本文档写明的内容。
4. **不提交代码**。我会在 VS Code + Live Server 本地测试后自己 commit。
5. 不新增 localStorage key,信的内容每次从 sessions 实时计算,**不存文本**。
6. 日期一律用本地日历日,对齐现有代码的 `toLocaleDateString('sv')` 写法。

## 设计决策(已全部确认)

### 概念

书读完时,页焰替这本书留下一封简短的告别信。信不强迫读,读完可以随时回来重读——朋友离开了,信还在书里。

### 信的内容(产品旁白口吻,第二人称,3–4 句)

按顺序由以下几句组成:

1. **开头**:`《书名》读完了。`
2. **陪伴句**(天数 = 该书有阅读 session 的**去重本地日历日**数):
   - 阅读日跨越多个月:`它陪了你 14 天,从三月走到五月。`(中文月份;若跨年,写成 `从 2025 年 12 月走到 2026 年 1 月`)
   - 阅读日在同一个月内:`这个月,它陪伴你度过了 14 天。`(若完读时已不是那个月,改说 `那段时间,它陪伴你度过了 14 天。`)
3. **停顿句**(条件句):最长停顿 = 相邻两个阅读日的日期差减一(即中间完全没读的天数)。仅当最长停顿 **≥7 天**时出现:`中间有过一次 11 天的停顿,你还是回来了。`
4. **收尾**(固定):`合上书,道个别吧。`

硬性原则:**没数据的句子不出现,绝不硬凑**。该书没有任何 session(比如添加时直接标 100%)→ 整封信不存在,所有入口都不显示。

### 入口一:完读结算流程

- 位置:结算页第四层完读庆祝(`recapFinishLayer`)内,追加一行安静的文字入口:`这本书给你留了一封信 →`
- 点开 → 全屏的信;关上 → 回到结算页,流程继续(第五层下一本建议不受影响)。
- 不点也不打断流程,结算页照常走完。
- 注意:详情页"完读"按钮那条路径的庆祝遮罩(`showFinishCelebration`)**保持原样不动**——用户关掉遮罩后回到详情页,详情页本身就有信的入口(见下),覆盖到了。

### 入口二:书籍详情页

- 仅 `status === 'finished'` 且该书有 session 的书,详情页出现入口:`重新翻出这封信 →`
- 点开复用同一个全屏信。

### 全屏信的形态

- 全屏 overlay(可参考现有 `.finish-celebration-content` 遮罩的实现模式),深色背景,信的几句话竖排、行距宽松、居中或左对齐皆可,以安静为准。
- 底部一个关闭按钮(文案用 `合上` 即可),点击关闭返回来处(结算页或详情页)。
- 无音效、无庆祝动画,至多整体淡入。

## 实现步骤

### Step 1:数据层

在 `app.js` 增加 `getFarewellLetter(book)`:
- 读该书全部 sessions,计算:去重阅读日数、首末阅读日的月份跨度、最长停顿天数。
- 按上面的规则拼句,返回 `string[]`(每句一个元素);该书无 session 返回 `null`。
- 纯函数,不做 DOM 操作。

**验证**:控制台对现有完读书调用;再用 mock 数据分别构造同月 / 跨月 / 跨年 / 停顿 ≥7 天 / 无 session 五种情况,确认输出正确,测完还原数据。

### Step 2:全屏信 UI + 详情页入口

- `index.html` 增加全屏信 overlay 结构;`style.css` 配套样式。
- 详情页(`renderBookDetail`)对符合条件的书渲染 `重新翻出这封信 →` 入口,点击打开全屏信,`合上` 返回详情页。

**验证**:找一本已完读的书进详情页,开信、读信、合上;再看一本未完读的书和一本无 session 的完读书,确认入口不出现。

### Step 3:结算页入口

- `showFinishLayer` 中,当 `getFarewellLetter` 有内容时,在完读庆祝层里渲染 `这本书给你留了一封信 →`,点击打开全屏信,合上后回到结算页,后续层与"继续"按钮不受影响。
- 用户提前点"继续"清掉 `pendingRecap` 的现有守卫逻辑保持不变。

**验证**:走一次真实完读流程(可用 mock 数据把一本书推到 99% 再计时完读):看入口出现、开信合上、第五层照常出现、点"继续"正常收尾。

### Step 4:回归

- 结算页五层原有时序不受影响;跳过/完成/继续按钮行为不变。
- 详情页其他功能(完读/弃读/重新开始读)无异常。
- 提醒:部署后 iPad PWA 需移除再重新添加主屏图标;`sw.js` 缓存版本号要更新。
